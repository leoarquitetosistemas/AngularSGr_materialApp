import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.SimonGrimmMaterialApp',
  appName: 'material-app',
  webDir: 'dist/material-app',
  bundledWebRuntime: false
};

export default config;
